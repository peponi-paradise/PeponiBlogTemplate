---
title: Code
category: Markdown
description: Code를 이용해 code block을 표현할 수 있다.
date: 2024-10-08T00:00:00+09:00
tags: ["Syntax", "<pre>", "<code>", "Code block"]
---

## 1. Introduction

- 단어 또는 구를 `` ` (Backtick) `` 기호를 이용하여 code로 지정

## 2. Example

<table>
    <thead>
        <tr>
            <th>Code</th>
            <th>Render</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>
            ```md title="Markdown"
            간단한 `코드` 표시
            ```
            </td>
            <td>
            간단한 `코드` 표시
            </td>
        </tr>
        <tr>
            <td>
            ```md title="Markdown"
            <!-- 코드 내에 ` 기호가 있는 경우 -->

            `` 코드 중간에 ` 기호 ``
            ```
            </td>
            <td>
            `` 코드 중간에 ` 기호 ``
            </td>
        </tr>
        <tr>
            <td>
            ````md title="Markdown"
            <!-- 코드 블록 표현은 backtick 3개 (```) -->
            <!-- 언어 지정 가능 -->

            ```python
            def TestFunction(PrintString:str):
                print(PrintString)
            ```
            ````
            </td>
            <td>
            ```python
            def TestFunction(PrintString:str):
                print(PrintString)
            ```
            </td>
        </tr>
    </tbody>
</table>

## 3. 참조 자료

- [Code](https://www.markdownguide.org/basic-syntax/#code)